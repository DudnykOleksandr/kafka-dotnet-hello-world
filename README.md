# kafka-dotnet-hello-world

> This is a simple kafka producer and consumer using dotnet core and docker

## Steps

1. Clone repository

    ```bash
    git clone 
    ```
	
2. Run docker compose

    ```bash
    docker-compose up
    ```